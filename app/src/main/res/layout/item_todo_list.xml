<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools">

    <data>
        <import type="com.sujungp.todoex.todolist.TodoListViewModel"/>
        <variable
                name="item"
                type="com.sujungp.todoex.data.TodoItem" />

        <variable
                name="viewModel"
                type="com.sujungp.todoex.todolist.TodoListViewModel" />
    </data>

    <androidx.cardview.widget.CardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:foreground="?attr/selectableItemBackground"
            android:orientation="vertical"
            app:cardCornerRadius="8dp"
            app:cardElevation="5dp"
            app:cardUseCompatPadding="true"
            app:contentPadding="10dp">

        <androidx.constraintlayout.widget.ConstraintLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content">

            <FrameLayout
                    android:id="@+id/todoStatusView"
                    android:layout_width="50dp"
                    android:layout_height="50dp"
                    app:layout_constraintBottom_toBottomOf="parent"
                    app:layout_constraintLeft_toLeftOf="parent"
                    app:layout_constraintTop_toTopOf="parent">

                <com.airbnb.lottie.LottieAnimationView
                        android:id="@+id/todoStatus"
                        android:layout_width="30dp"
                        android:layout_height="30dp"
                        android:layout_gravity="center"
                        android:onClick="@{() -> viewModel.updateTodoStatus(item.id, item.todoStatus)}"
                        app:setStatus="@{item.todoStatus}"
                        app:lottie_fileName="lottie/checkbox.json" />
            </FrameLayout>

            <TextView
                    android:id="@+id/todoTitle"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:text="@{item.todoTitle}"
                    android:textAppearance="?android:textAppearanceLarge"
                    app:layout_constraintLeft_toRightOf="@id/todoStatusView"
                    app:layout_constraintRight_toRightOf="parent"
                    app:layout_constraintTop_toTopOf="parent"
                    tools:text="제목" />

            <TextView
                    android:id="@+id/todoDesc"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="5dp"
                    android:ellipsize="end"
                    android:maxLines="2"
                    android:text="@{item.todoDesc}"
                    android:textAppearance="?android:textAppearanceMedium"
                    app:layout_constraintLeft_toRightOf="@id/todoStatusView"
                    app:layout_constraintRight_toRightOf="parent"
                    app:layout_constraintTop_toBottomOf="@id/todoTitle"
                    tools:text="내용" />

            <TextView
                    android:id="@+id/todoDate"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="5dp"
                    android:text="@{item.todoTargetDate}"
                    android:textAppearance="?android:textAppearanceMedium"
                    app:layout_constraintLeft_toRightOf="@id/todoStatusView"
                    app:layout_constraintTop_toBottomOf="@id/todoDesc"
                    tools:text="2019.09.02" />

        </androidx.constraintlayout.widget.ConstraintLayout>
    </androidx.cardview.widget.CardView>
</layout>